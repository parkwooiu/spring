<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                                 http://www.springframework.org/schema/beans/spring-beans.xsd
                                 http://www.springframework.org/schema/security
                                 http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- URL 접근 권한 설정 -->
    <http auto-config="true">
        <!-- /sample/all에 대한 접근 권한 설정 -->
        <intercept-url pattern="/sample/all" access="permitAll"/>
        <!-- /sample/user에 대한 접근 권한 설정 -->
        <intercept-url pattern="/sample/user" access="hasRole('ROLE_USER')"/>
        <!-- /sample/admin에 대한 접근 권한 설정 -->
        <intercept-url pattern="/sample/admin" access="hasRole('ROLE_ADMIN')"/>

        <!-- 로그인 페이지 및 로그인 성공 후 이동할 URL 설정 -->
        <form-login login-page="/customLogin" default-target-url="/home" authentication-failure-url="/customLogin?error=true"/>
        <!-- 로그아웃 설정 -->
        <logout logout-success-url="/login"/>
    </http>

    <!-- 사용자 권한 설정 -->
    <authentication-manager>
        <!-- In-memory 사용자 설정 -->
        <authentication-provider>
            <user-service>
                <user name="user" password="{noop}testPassword" authorities="ROLE_USER"/>
                <user name="admin" password="{noop}testPassword" authorities="ROLE_ADMIN"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>